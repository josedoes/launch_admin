import 'package:flutter/cupertino.dart';

const paddingHorizontal = 24.0;
const paddingHorizontalWeb = 32.0;

const insetsHorizontal = EdgeInsets.symmetric(horizontal: paddingHorizontal);
const insetsHorizontalDesktop = EdgeInsets.symmetric(horizontal: paddingHorizontalWeb);
